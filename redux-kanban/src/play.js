import {createStore} from 'redux'

//import categoryReducer from './reducer/category.js'

//import {
  //categoryCreate,
  //categoryUpdate,
  //categoryDelete,
  //categoryReset,
//} from './action/category-actions.js'

//const store = createStore(categoryReducer)

//every time dispatch is done, it will call subscribe

//store.subscribe(() => {
//console.log('__STATE__', store.getState())
//})

//store.dispatch(categoryCreate({title: 'lulwat sweet'}))
//store.dispatch(categoryCreate({title: 'i love redux'}))
//store.dispatch(categoryCreate({title: 'js rules'}))
